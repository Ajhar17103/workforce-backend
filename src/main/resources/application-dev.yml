spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwt
    username: root
    password: Admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8086
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
